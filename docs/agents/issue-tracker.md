# GitHub issue-tracker contract

Repository: `GlitchTrader/glitch-crypto-hermes-profile`.

- `operations/ledger.json` is the current-work authority.
- GitHub issues carry durable requests and unresolved decisions; the gateway map may own cross-repository sequencing.
- Direct delivery is to `main`; create a pull request only on explicit request.
- `to-ticket` is the user's alias for upstream `to-tickets`.
- Use native sub-issue and dependency links for genuine Wayfinder work.
- Close completed or superseded issues with the source commit and Rail item.
- Record gateway dependencies in Rail extensions, not as nonexistent local item IDs.
